# Base application env variables.
# Copy to .env and edit. Every variable listed here should be declared in
# the relevant app's config schema (backend: ADR 0021; web: ADR 0019).

NODE_ENV=development
LOG_LEVEL=info
