# Allowlist of env variables exposed to the browser bundle.
# Per ADR 0016, only variables listed here can be referenced via
# `import.meta.env.*` in client code. Values here are placeholders;
# real values are supplied per-environment at deploy time.
API_URL=http://localhost:3000
