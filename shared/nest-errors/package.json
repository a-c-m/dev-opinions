{
  "name": "@shared/nest-errors",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure-type stub per ADR 0039 — only type/interface exports, no runtime emission. Adds `build` + `dist/` shape when the first runtime export lands.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "nx": {
    "projectType": "library",
    "tags": [
      "scope:shared",
      "type:lib"
    ]
  },
  "scripts": {
    "lint": "bs check --write .",
    "test": "vitest run --passWithNoTests",
    "test:cov": "vitest run --coverage --passWithNoTests",
    "test:watch": "vitest",
    "typecheck": "tsgo --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@shared/contracts": "workspace:*"
  }
}
